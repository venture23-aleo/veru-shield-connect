{
  "name": "strk20-messaging",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "web": "pnpm --filter @strk20-messaging/web dev",
    "web:devenv": "node scripts/gen-web-env.mjs",
    "web:build": "pnpm --filter @strk20-messaging/web build",
    "web:preview": "pnpm --filter @strk20-messaging/web preview",
    "cli": "node apps/cli/dist/index.js",
    "vectors": "node vectors/generate.mjs",
    "web:devnet": "node scripts/pool-devnet.mjs",
    "sdk:patch": "node scripts/patch-privacy-sdk.mjs"
  }
}
